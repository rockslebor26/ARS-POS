name: ARS POS - Build APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    name: Build ARS POS Release APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Extract ARS POS source
        run: |
          unzip -q ARS_POS_1.0.0_RELEASE_CANDIDATE.zip
          echo "Source ARS POS berhasil diekstrak"
          ls -la ARS_POS_Phase2

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK components
        run: |
          sdkmanager "platform-tools"
          sdkmanager "platforms;android-35"
          sdkmanager "build-tools;35.0.0"

      - name: Build Release APK
        working-directory: ARS_POS_Phase2
        run: |
          gradle clean :app:assembleRelease --stacktrace

      - name: Check APK
        run: |
          ls -lh ARS_POS_Phase2/app/build/outputs/apk/release/

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ARS-POS-1.0.0-RELEASE
          path: ARS_POS_Phase2/app/build/outputs/apk/release/*.apk
          if-no-files-found: error
